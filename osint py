import phonenumbers
from phonenumbers import geocoder, carrier, timezone
import socket, re, webbrowser

# COLOUR CODES
R = "\033[91m"
G = "\033[92m"
Y = "\033[93m"
C = "\033[96m"
W = "\033[97m"

print(R + """
╔══════════════════════════════════════╗
║ 👻 PRANAY GHOST OSINT V5.0 ULTRA ║
║ Bhandara Cyber Squad 💀 ║
╚══════════════════════════════════════╝
""" + W)
print(G + " [ 4-in-1 Tool: Phone | Email | IP | Insta ]" + W)

def phone_osint():
    num = input(Y + "\n📱 Phone Number (+91 ke saath): " + W)
    try:
        p = phonenumbers.parse(num)
        print(G + "✅ Valid:" + W, phonenumbers.is_valid_number(p))
        print("📶 Operator:", carrier.name_for_number(p, 'en'))
        print("📍 Location:", geocoder.description_for_number(p, 'en'))
        print("⏰ Timezone:", timezone.time_zones_for_number(p))
        print(C + "🔎 Link: truecaller.com/search/in/" + num + W)
    except:
        print(R + "❌ Galat Number!" + W)

def email_osint():
    email = input(Y + "\n📧 Email daal: " + W)
    if re.match(r"[^@]+@[^@]+\.[^@]+", email):
        print(G + "✅ Valid:" + W, email)
        print("🌐 Domain:", email.split('@')[1])
        print(C + "🔎 Leak: haveibeenpwned.com" + W)
    else:
        print(R + "❌ Invalid Email" + W)

def ip_osint():
    ip = input(Y + "\n🌐 IP daal
